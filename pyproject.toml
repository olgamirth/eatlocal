[project]
name = "eatlocal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.47.0",
    "python-decouple>=3.8",
    "typer>=0.12.5",
]
